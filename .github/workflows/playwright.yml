name: Playwright Tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: |
          cd nopcommerce-exhaustive-testing
          npm install

      - name: Install Playwright browsers
        run: |
          cd nopcommerce-exhaustive-testing
          npx playwright install --with-deps

      - name: Run tests
        run: |
          cd nopcommerce-exhaustive-testing
          npx playwright test
